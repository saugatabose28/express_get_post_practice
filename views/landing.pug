extends layout

block content
  h1= title
  p Welcome to #{title}
  form("/",method="post")
   input(name="lead_email",type="email", placeholder="Enter your email address")
   button(type="submit") Submit
